#ifndef __CAR_COUNT_H__
#define __CAR_COUNT_H__

#include "multi_video_processor.h"
#include "setting.h"
#include <time.h>
#include <math.h>


/*
class FrameStatInfo {
public:
	int count;
	double avg_speed;
	double occupancy;
	vector<double> speed_per_lane; 

	FrameStatInfo(int lanes) {
		count = 0;
		occupancy = 0.0;
		avg_speed = 0.0;
		for(int i=0; i<lanes; i++) {
			speed_per_lane.push_back(0.0);
		}
	}
};
*/


#endif